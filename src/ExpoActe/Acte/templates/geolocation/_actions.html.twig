<div class="row gy-2 gx-1 align-items-center d-print-none mb-3">
    <div class="col-auto">
        <a href="{{ path('expoacte_geolocation_edit',{'id':geolocation.id}) }}" class="btn btn-primary"
           role='button'>
            <i class="ti ti-pencil"></i> Modifier
        </a>
    </div>
    <div class="col-auto">
        <a href="{{ path('expoacte_geolocation_index') }}" role="button" class="btn btn-primary">
            <i class="ti ti-arrow-left"></i> Retour à la liste
        </a>
    </div>
    <div class="col-auto">
        <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
            <i class="ti ti-trash"></i> Supprimer
        </button>
    </div>
</div>