<!-- Modal candidat -->
<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel"
     aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">Supprimer le paramètre</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Supprimer {{ parameter.name }} ?
            </div>
            <div class="modal-footer">
                <form method="post" action="{{ path('expoacte_parameter_delete', {'uuid': parameter.uuid}) }}">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parameter.uuid) }}">
                    <button type="button" class="btn btn-secondary"
                            data-bs-dismiss="modal">Annuler
                    </button>
                    <button type="submit" class="btn btn-danger">Supprimer le paramètre</button>
                </form>
            </div>
        </div>
    </div>
</div>
