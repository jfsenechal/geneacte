{% extends '@ExpoActe/base.html.twig' %}

{% block body %}

    <div class="card">
        <h3 class="card-header">Ajout d'un acte {{ certificateType.label }}</h3>

        <div class="card-body">
            {% include '@ExpoActe/certificate/form/_form.html.twig' %}
        </div>

        <div class="card-footer text-end">
            <a href="{{ path('expoacte_home') }}" class="btn btn-secondary">
                <i class="ti ti-arrow-left"></i> Annuler
            </a>
        </div>
    </div>

{% endblock %}
