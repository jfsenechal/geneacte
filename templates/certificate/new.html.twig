{% extends '@ExpoActe/base.html.twig' %}

{% block body %}

    <div class="card">
        <h3 class="card-header">Ajout d'un acte</h3>

        <div class="card-body">
            {{ form(form) }}
        </div>
        <div class="card-footer text-end">
            <a href="{{ path('geneacte_certificate_birth_index') }}" class="btn btn-secondary">
                <i class="fa fa-arrow-left"></i> Annuler
            </a>
        </div>
    </div>

{% endblock %}
