<div class="row gy-2 gx-1 align-items-center d-print-none mb-3">
    <div class="col-auto">
        <a href="{{ path('geneacte_user_edit',{'uuid':user.uuid}) }}" class="btn btn-primary"
           role='button'>
            <i class="ti ti-pencil"></i> Modifier
        </a>
    </div>
    <div class="col-auto">
        <a href="{{ path('geneacte_user_password',{'uuid':user.uuid}) }}" class="btn btn-primary"
           role='button'>
            <i class="ti ti-password-user"></i> Changer le mot de passe
        </a>
    </div>
    <div class="col-auto">
        <a href="{{ path('geneacte_user_index') }}" role="button" class="btn btn-primary">
            <i class="ti ti-arrow-left"></i> Retour à la liste
        </a>
    </div>
    <div class="col-auto">
        <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
            <i class="ti ti-trash"></i> Supprimer
        </button>
    </div>
</div>